import styled from 'styled-components';

export const SidebarBody = styled.aside`
  width: 250px;
`;

export const SidebarInner = styled.div`
  position: sticky;
  top: 0;
  border-radius: 4px;
  background: rgba(0,0,0,0.60);
  box-shadow: 6px 0 10px 10px rgba(0,0,0,0.03);
  padding: 25px;
`;

export const SidebarTag = styled.div`
  display: flex;
`;